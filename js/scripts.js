// Business logic
function Pizza(size, toppings) {
  this.size = size;
  this.topping1 = topping1;
  this.topping1 = topping2;
  this.topping1 = topping3;
  this.topping1 = topping4;
}

Pizza.prototype.pSize = function() {
  var sizePrice = 0;
  if (this.size === small) {
    sizePrice = 5;
  } else if {this.size === medium) {
    sizePrice = 6;
  } else if {this.size === medium) {
    sizePrice = 8;
  } else if {this.size === medium) {
    sizePrice = 10;
  };

Pizza.prottype.pTopping1 = function() {
  var topping1Price = 0;
  if (this.size === small) {
    topping1Price = 5;
  } else if {this.size === medium) {
    sizePrice = 6;
  } else if {this.size === medium) {
    sizePrice = 8;
  } else if {this.size === medium) {
    sizePrice = 10;
  };

Pizza.prottype.pTopping2 = function() {
  var topping2Price = 0;
  if (this.topping1 === small) {
    topping1Price = 5;
  } else if {this.size === medium) {
    sizePrice = 6;
  } else if {this.size === medium) {
    sizePrice = 8;
  } else if {this.size === medium) {
    sizePrice = 10;
  };

Pizza.prottype.pTopping2 = function() {
  var topping3Price = 0;
  if (this.size === small) {
    topping1Price = 5;
  } else if {this.size === medium) {
    sizePrice = 6;
  } else if {this.size === medium) {
    sizePrice = 8;
  } else if {this.size === medium) {
    sizePrice = 10;
  };

Pizza.prottype.pTopping2 = function() {
  var topping41Price = 0;
  if (this.size === small) {
    topping1Price = 5;
  } else if {this.size === medium) {
    sizePrice = 6;
  } else if {this.size === medium) {
    sizePrice = 8;
  } else if {this.size === medium) {
    sizePrice = 10;
  };
}



// User interface logic
$(document).ready(function() {
  event.preventDefault();

  $("form#order-form").submit(function(event) {
  event.preventDefault();

}
